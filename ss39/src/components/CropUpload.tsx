// Demo placeholder - thực tế dùng thư viện react-easy-crop hoặc react-image-crop
export default function CropUpload() {
  return (
    <div>
      <h2>Crop Upload</h2>
      <p>Ở đây bạn có thể crop ảnh trước khi upload (sẽ dùng thư viện crop).</p>
    </div>
  );
}
